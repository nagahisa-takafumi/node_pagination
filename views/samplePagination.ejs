<!DOCTYPE html>
<html lang="ja" class="h-100">

<head>
  <title>Sample Pagination</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</head>

<body class="d-flex flex-column h-100">
  <main class="mt-5">
    <section class="container w-50">
      <h1 class="m-auto text-center mb-3">Sample Pagination <%= currentPage %></h1>
      <table class="table w-75 m-auto mb-3">
        <thead>
          <tr>
            <th class="tableMenu">Number</th>
          </tr>
        </thead>
        <tbody>
          <% for (let i=0; i < data.length; i++) { %>
            <tr>
              <td><%= data[i] %></td>
            </tr>
          <% } %>
        </tbody>
      </table>
      <ul class="pagination justify-content-center m-auto">
        <li class="page-item <%= currentPage == 1 ? 'disabled' : '' %>">
          <a href="<%= currentPage - 1 %>" class="page-link">Prev</a>
        </li>
        <% for (let i=0; i < pageLength; i++) { %>
          <li class="page-item<%= i == currentPage - 1 ? ' active' : '' %>">
            <a href="<%= i + 1 %>" class="page-link"><%= i + 1 %></a>
          </li>
        <% } %>
        <li class="page-item <%= currentPage == pageLength ? 'disabled' : '' %>">
          <a href="<%= currentPage + 1 %>" class="page-link">Next</a>
        </li>
      </ul>
    </section>
  </main>
</body>

</html>
